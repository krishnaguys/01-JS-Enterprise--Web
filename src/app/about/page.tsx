import { SectionTitle } from "@/components/common/SectionTitle";

export default function About() {
  return (
    <div className="container section-padding">
      <SectionTitle title="About Us" subtitle="Learn more about our mission" />
    </div>
  );
}