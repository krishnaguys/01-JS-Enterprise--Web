import { SectionTitle } from "@/components/common/SectionTitle";

export default function Gallery() {
  return (
    <div className="container section-padding">
      <SectionTitle title="Gallery" subtitle="View our latest work" />
    </div>
  );
}